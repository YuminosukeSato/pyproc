apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pyproc.fullname" . }}-test-connection"
  labels:
    {{- include "pyproc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['--spider', '--timeout=5', '{{ include "pyproc.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
