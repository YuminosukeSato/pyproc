# EKS IRSA values override for pyproc
# Configures ServiceAccount annotation for IAM Roles for Service Accounts.

serviceAccount:
  create: true
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::ACCOUNT_ID:role/ROLE_NAME"
