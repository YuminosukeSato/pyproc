version: "2"
run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - revive
    - misspell
    - errcheck
  exclusions:
    rules:
      - path: ^bench/
        linters:
          - errcheck
          - revive
          - staticcheck
