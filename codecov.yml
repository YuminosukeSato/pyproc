codecov:
  require_ci_to_pass: true

coverage:
  range: "100..100"
  status:
    project:
      default:
        target: 100
        threshold: 0
    patch:
      default:
        target: 100
        threshold: 0

comment: false
