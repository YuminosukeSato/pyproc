codecov:
  require_ci_to_pass: true

coverage:
  range: "70..100"
  status:
    project:
      default:
        target: 80
        threshold: 5
    patch:
      default:
        target: 70
        threshold: 10

comment: false
